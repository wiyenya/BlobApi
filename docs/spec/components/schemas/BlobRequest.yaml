allOf:
  - $ref: '#/components/schemas/BlobKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        properties:
          value:
            description: arbitrary text
            type: string
            example: foobar
          UserID:
            type: integer
            format: int
            example: 2
        required:
          - value

